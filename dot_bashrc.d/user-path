# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
  PATH="$HOME/bin:$PATH"
fi

if [ -d "$HOME/dev/golang/bin" ] ; then
  PATH="$PATH:$HOME/dev/golang/bin"
fi

if [ -d "/usr/local/go/bin" ] ; then
  PATH="$PATH:/usr/local/go/bin"
fi
