# Jump back n directories at a time
alias ..='cd ..'
alias ...='cd ../../'
alias ....='cd ../../../'
alias .....='cd ../../../../'
alias ......='cd ../../../../../'
alias cdc="cd ${HOME}/.config"

# Make and cd into directory
function mcd() {
  mkdir -p "$1" && cd "$1";
}
